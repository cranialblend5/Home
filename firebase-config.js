// ---------------------------------------------------------------------------
// Fill this in with YOUR Firebase project's web config (see README.md,
// step "1. Create a free Firebase project"). These values are NOT secret —
// they identify which Firebase project to talk to, they don't grant access
// on their own (Firestore security rules control who can read/write).
// ---------------------------------------------------------------------------
const firebaseConfig = {
  apiKey: "PASTE_API_KEY_HERE",
  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  storageBucket: "PASTE_PROJECT_ID.appspot.com",
  messagingSenderId: "PASTE_SENDER_ID",
  appId: "PASTE_APP_ID"
};
